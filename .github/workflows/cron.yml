name: cron
on:
  schedule:
    - cron: '* */4 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call the cron API route
        run: |
          curl --request POST \
          --url 'https://qotqot-git-cron-comicscrip.vercel.app/api/cron?key=${{ secrets.API_SECRET_KEY }}'